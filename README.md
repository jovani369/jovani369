# Starter Python CLI

This is a minimal starter template for a Python command-line interface (CLI).

Quickstart

1. Run the CLI locally:

   python3 main.py greet Alice

2. Show version:

   python3 main.py --version

Files
- main.py: The CLI entry point using argparse
- requirements.txt: Python dependencies (empty for now)
- .gitignore: Common ignores

License: MIT (add your preferred license)
